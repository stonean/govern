# Inbox

- Runtime's `gvrn exec` command-file lookup hardcodes `.claude/commands/gov/`. `runtime/src/main.rs:207-218` falls back through `framework/commands/*.md`, `.claude/commands/gov/*.md`, `framework/bootstrap/*.md` when resolving the procedure file for `gvrn exec <command>`. The middle path bakes in both Claude Code's config dir (`.claude/`) and this project's name (`gov`). Adopters on Auggie (`.augment/commands/{project}/`) or any non-`gov` project name miss the lookup and fall back to the framework path — fine in this repo (the framework dir exists at the source) but broken in an adopter project that only has the slash command files under their host's config dir. Surfaced 2026-05-24 during the `.govern.session.toml` consolidation sweep on spec 022; left out of scope because the fix is independent of session state and the design is non-obvious (env var? `.govern.toml` config? CLI flag? bootstrap-written runtime config? — needs its own scenario or spec to pick a shape).

- Runtime's interpreter `locate_command_file` hardcodes `.claude/commands/gov/` for the same reasons as above. `runtime/src/interpreter/payload.rs:378-390` resolves command files when extracting anchor references (`Reference: §<anchor>` lines in the Scope Boundaries block). Same bug shape as the `gvrn exec` lookup: works in this repo because the source `framework/commands/*.md` always wins the search, but an adopter project relying on the host-config-dir fallback hits the wrong path. Pair this with the `gvrn exec` finding above — same fix (parameterize the commands-dir lookup), one scenario or spec, two callsites. Surfaced 2026-05-24 during the same sweep.

- Architectural exploration: re-frame the runtime's LLM extension points (`writeCode`, `writeSpecBody`, `assessSpecQuality`, future multi-turn points) as named Anthropic-style Skills the host loads at the seam, rather than ad-hoc JSON envelopes. Potential benefits: structural cache anchoring (Skills are a natural cache boundary); third-party hosts integrate against an emerging Skills protocol instead of govern-specific JSON; `constitution-excerpts` becomes a bundled resource rather than an inline string array. Speculative — depends on Anthropic's Skills protocol stabilizing and is a larger redesign than 022's current scope. Revisit after the writeCode payload-bundling scenario on 022 ships and the cache-anchored shape proves out the pattern. Surfaced 2026-05-19 during runtime-improvement investigation.

- New rule file `framework/rules/quality-cross.md` (per `constitution.md:306` "new rule files are introduced via their own feature spec"): introduce a cross-cutting code-quality category with `QUAL-STUB-001` as the inaugural rule — *partial or unimplemented code paths MUST fail loudly (panic / explicit error / failing test fixture) rather than silently pass through; stubs that return zero values, no-op middleware that returns `next` unchanged, handlers that return early without an error, and methods that return `nil, nil` are forbidden when the surrounding contract implies the path performs work.* Rationale: silent stubs ship indistinguishably from working implementations — a no-op rate-limiter, an always-allow permission check, a publisher that drops events on the floor — and the failure mode surfaces only when the missing behavior is needed, which is precisely when the system is under stress. None of the existing rule files cover this discipline (`api-backend.md` is closest via `BE-SCHEMA-002`'s "fail loudly at build time" rationale, but the principle is genuinely cross-cutting — applies to background workers, domain methods, frontend stores, anything with a contract). New feature spec also needs the manifest entry in `framework/bootstrap/govern.md` (slot between `performance-frontend.md` and `security-backend.md`); rule-loader infrastructure from spec 024 picks it up automatically via the `-cross.md` suffix. Motivating incident: 008-rate-limiting Task 4 in the anvil project (a passthrough stub in `RateLimit` enabled-mode would have silently disabled rate limiting in production if Task 5 were skipped; the team manually added a panic with a "not implemented" message until Task 5 landed — a discipline the framework should make automatic). Surfaced 2026-05-23 via `/anvil:groom` on the adopter project; the anvil item stays in `specs/inbox.md` there until this rule lands upstream and syncs down.
