# Inbox

- Primitive proposal: `next-task` — composes `read-tasks` + first-incomplete-task lookup + plan-relevant-files for that task into one MCP call. Reduces /gov:implement's per-task MCP round-trips from 3 to 1. Defer until the writeCode payload-bundling scenario on 022 lands and wall-clock + token-cost deltas are measured; this primitive may be unnecessary if Claude Code's host-side caching already covers the per-task gather. Surfaced 2026-05-19 during runtime-improvement investigation.
- Primitive proposal: `gather-cross-spec-context` — for /gov:plan, deterministically reads `specs/system.md`, conditionally `events.md` (if spec body mentions events) and `errors.md` (if spec introduces error codes), and dependency spec files from frontmatter `dependencies`. Mirrors the cross-spec context checklist already prose-encoded in framework/commands/plan.md. Defer until the writeCode payload-bundling scenario on 022 lands and is measured; if host-side caching of these stable files already covers it, this primitive may not pay its weight. Surfaced 2026-05-19 during runtime-improvement investigation.
- Architectural exploration: re-frame the runtime's LLM extension points (`writeCode`, `writeSpecBody`, `assessSpecQuality`, future multi-turn points) as named Anthropic-style Skills the host loads at the seam, rather than ad-hoc JSON envelopes. Potential benefits: structural cache anchoring (Skills are a natural cache boundary); third-party hosts integrate against an emerging Skills protocol instead of govern-specific JSON; `constitution-excerpts` becomes a bundled resource rather than an inline string array. Speculative — depends on Anthropic's Skills protocol stabilizing and is a larger redesign than 022's current scope. Revisit after the writeCode payload-bundling scenario on 022 ships and the cache-anchored shape proves out the pattern. Surfaced 2026-05-19 during runtime-improvement investigation.
