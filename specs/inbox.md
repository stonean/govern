# Inbox

- **`/audit` Family 8 — past-tense rewrites for old-name references in done specs:** `scripts/audit/introducing-drift.sh` flags ~9 done specs (011, 014, 017, 020, 021, 022, 023, 024, 000) that retain backticked references to renamed commands (`/capture`, `/elaborate`, `/validate`, `gov-rt:`) in current-tense prose. Each rewrite is a small `/gov:ask` cycle on the affected spec — append a clarify-question, accept the past-tense rewrite. Spec body language shifts from "A new `/capture` command provides..." to "`/capture` provided..." (or replaces with the new name's past-tense form where applicable). Per the design, this is intentionally maintainer-paced — the audit surfaces it, the maintainer addresses it organically when they touch the affected specs for other reasons. (Origin: spec 026 /audit v1 advisory findings, 2026-05-18.)

- **`/audit` Family 9 — annotate or promote prose-only steps in rewritten commands:** `scripts/audit/primitive-promotion-candidates.sh` flags numbered Instructions steps in the six rewritten commands (analyze, implement, plan, specify, status, target) that have neither a backticked runtime-primitive name nor an `<!-- llm:* -->` extension-point marker. Each is either a host-responsibility step (needs `<!-- audit:ignore-promotion -->` annotation on the preceding content line) or a primitive-promotion candidate (deterministic logic that should become a new gvrn primitive). Two-fold work: annotate the host-responsibility steps inline; design new primitives for the rest. The latter expands the runtime; the former is mechanical. Probably ships as two separate scenarios — one annotation sweep, one batch of primitive promotions. (Origin: spec 026 /audit v1 advisory findings, 2026-05-18.)

- **`/audit` CI: flip `continue-on-error` to false once Family 4/8/9 close:** `.github/workflows/markdown-only-pipeline.yml` step (h) and `.github/workflows/runtime-release.yml`'s `audit` job both run `/audit` with `continue-on-error: true` in v1 soft-launch mode. Once the framework drift in Families 4, 8, and 9 is resolved (the three items above), remove the `continue-on-error` flags so `/audit` becomes a hard PR / release gate per spec 026 Q4. (Origin: spec 026 Phase D CI integration, 2026-05-18.)

- **REUSE-001: extract `scripts/audit/lib.sh` for shared boilerplate:** Each of the nine `scripts/audit/*.sh` family check scripts repeats ~10 lines of identical setup (`set -uo pipefail`, `ROOT=...`, `cd "$ROOT"`, `drift=0`, the `emit()` function with pipe-separated output). A shared `scripts/audit/lib.sh` exposing `audit_emit FAMILY LOCATION MESSAGE FIX` plus the ROOT/cd setup would eliminate ~90 lines of duplication. Trade-off: shared lib couples the scripts and reduces stand-alone-invocation ergonomics (`bash scripts/audit/X.sh` would require lib.sh on a relative path). Documented in `scripts/audit/README.md` as the v1 trade-off; extracting is optional cleanup, not a blocker. (Origin: spec 026 review SHOULD finding, 2026-05-18.)

- **QUALITY-001: refactor `flush_step` in primitive-promotion-candidates.sh to avoid caller-scope mutation:** `scripts/audit/primitive-promotion-candidates.sh`'s `flush_step()` function reads and mutates seven caller-scope variables (`step_start_line`, `step_buffer`, `step_has_primitive`, `step_has_llm_marker`, `step_has_ignore`, plus emit's `drift`). Bash semantics make this work (functions inherit caller scope by default), but the pattern is fragile to refactor — adding a `local` keyword anywhere in the function would silently break it. Confidence 70% — works as tested but brittle. Fix: convert the per-file loop into a function that returns the step list, then iterate the returned list to apply flush logic. (Origin: spec 026 review low-confidence finding, 2026-05-18.)

- **Claude Code prompts on `.claude/gov-session.json` writes despite explicit `Write(...)` allowlist entry:** Spec 023 added `Edit(.claude/gov-session.json)` and `Write(.claude/gov-session.json)` to `.claude/settings.local.json` so pipeline commands stop prompting on session-file writes. Commit c179776 added an AGENTS.md note instructing agents to use the `Write` tool (not Bash heredocs) so those entries actually apply. Observed 2026-05-17 during `/gov:specify` for spec 027 (invoked mid-`/gov:groom`): agent used the `Write` tool with both absolute path (`/Users/stonean/src/stonean/govern/.claude/gov-session.json`) and relative path (`.claude/gov-session.json`); both prompted. Every other `Write` in the same session (e.g., `specs/027-command-source-templating/spec.md`, `specs/inbox.md`) went through silently. The bare `Write` permission on line 4 of settings.local.json should additionally cover the call. Working hypothesis: the Claude Code harness has a built-in `.claude/`-directory protection that fires before `permissions.allow` is consulted, treating the whole directory as sensitive because settings live there. If confirmed, the per-path allow entries and the AGENTS.md note are no-ops — fix is either (a) escalate upstream to Claude Code to scope the protection to `.claude/settings*.json` only, or (b) move `gov-session.json` out of `.claude/` (e.g., `.govern/session.json`) and update spec 023's session-file path. Verify by: testing the same `Write` outside any Skill context; testing with `additionalDirectories` including `.claude/`; and checking Claude Code release notes / source for a `.claude/` directory protection. (Origin: 2026-05-17 `/gov:groom` session, item 2 of 7.)
