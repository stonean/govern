# Inbox

- Architectural exploration: re-frame the runtime's LLM extension points (`writeCode`, `writeSpecBody`, `assessSpecQuality`, future multi-turn points) as named Anthropic-style Skills the host loads at the seam, rather than ad-hoc JSON envelopes. Potential benefits: structural cache anchoring (Skills are a natural cache boundary); third-party hosts integrate against an emerging Skills protocol instead of govern-specific JSON; `constitution-excerpts` becomes a bundled resource rather than an inline string array. Speculative — depends on Anthropic's Skills protocol stabilizing and is a larger redesign than 022's current scope. Revisit after the writeCode payload-bundling scenario on 022 ships and the cache-anchored shape proves out the pattern. Surfaced 2026-05-19 during runtime-improvement investigation.

- Governance backfill — three rule files introduced without their own feature spec (violates `constitution.md` §rules "new rule files are introduced via their own feature spec"). `framework/rules/api-backend.md`, `accessibility-frontend.md`, and `performance-frontend.md` landed in commit 9ccbd0b bundled into specs 024 (rule-loader) and 025 (rule-opt-out); 024's spec body even notes they "have just landed." Their category abbreviations (`SCHEMA`/`APIVER`/`ERRENV`/`STATUS`/`PAGE`/`IDEMP`/`COMPAT`; `SEMHTML`/`KBD`/`ARIA`/`CONTRAST`/`A11YFORM`/`A11YMEDIA`; `VITALS`/`BUNDLE`/`IMAGE`/`LOAD`/`FONT`) were therefore never routed through the normal introducing-spec path. The schema/grammar half is now reconciled — constitution §rules states `specs/008-security-rules/data-model.md` (and `017` for config) own the ID grammar while each file's header owns its specific abbreviations, and `scripts/lint-rule-ids.sh` enforces the grammar in CI. Open decision: retroactive introducing specs for the three files (heavy, low value — the files work and 024/025 already adopted them) vs. a one-line constitution exception recording the backfill. Surfaced 2026-06-27 during a framework rule review.

- New rule file `framework/rules/quality-cross.md` (per `constitution.md:306` "new rule files are introduced via their own feature spec"): introduce a cross-cutting code-quality category with `QUAL-STUB-001` as the inaugural rule — *partial or unimplemented code paths MUST fail loudly (panic / explicit error / failing test fixture) rather than silently pass through; stubs that return zero values, no-op middleware that returns `next` unchanged, handlers that return early without an error, and methods that return `nil, nil` are forbidden when the surrounding contract implies the path performs work.* Rationale: silent stubs ship indistinguishably from working implementations — a no-op rate-limiter, an always-allow permission check, a publisher that drops events on the floor — and the failure mode surfaces only when the missing behavior is needed, which is precisely when the system is under stress. None of the existing rule files cover this discipline (`api-backend.md` is closest via `BE-SCHEMA-002`'s "fail loudly at build time" rationale, but the principle is genuinely cross-cutting — applies to background workers, domain methods, frontend stores, anything with a contract). New feature spec also needs the manifest entry in `framework/bootstrap/govern.md` (slot between `performance-frontend.md` and `security-backend.md`); rule-loader infrastructure from spec 024 picks it up automatically via the `-cross.md` suffix. Motivating incident: 008-rate-limiting Task 4 in the anvil project (a passthrough stub in `RateLimit` enabled-mode would have silently disabled rate limiting in production if Task 5 were skipped; the team manually added a panic with a "not implemented" message until Task 5 landed — a discipline the framework should make automatic). Surfaced 2026-05-23 via `/anvil:groom` on the adopter project; the anvil item stays in `specs/inbox.md` there until this rule lands upstream and syncs down.

- **Backend rule-set buildout (Task 9, continued from [034-performance-backend-rules](034-performance-backend-rules/spec.md)).** The constitution names performance / concurrency / observability as governance-recognized cross-cutting categories, but the backend surface covers only security (`security-backend.md`), API contract (`api-backend.md`), and now performance (`performance-backend.md`, spec 034). Three backend rule sets remain, each its own `/gov:specify`→clarify→plan→implement→review→done run (model after 034), `BE-` prefix with NEW categories disjoint from the existing BE- namespaces, design-time Verification clauses, registered in the `framework/bootstrap/govern.md` Shared Files manifest, and auto-selected under the `backend` surface via the `-backend.md` suffix (specs 024/033):
  - **`observability-backend.md`** — the rest of observability beyond `security-backend.md` §BE-LOG (which covers logging + audit). Candidate categories: metrics (RED/USE), distributed tracing (spans + propagation; extends `BE-LOG-006` correlation IDs), health endpoints (liveness/readiness/startup), SLOs / error budgets, alerting. Cross-ref `BE-LOG-005`/`BE-LOG-006` and `CFG-*` rather than restating.
  - **`concurrency-backend.md`** — zero coverage today; the constitution names concurrency twice. Candidate categories: shared-state races, locking (optimistic/pessimistic, lock ordering / deadlock avoidance), transaction isolation levels, idempotent retries (cross-ref `BE-IDEMP`), distributed coordination (distributed locks with fencing tokens, exactly-once vs at-least-once).
  - **`reliability-backend.md`** — **split out of 034 at clarify** (034's body forward-references it). Candidate categories: request deadlines + propagation, downstream-call timeouts, bounded retries with backoff+jitter (idempotent ops only — cross-ref `BE-IDEMP`), circuit breakers, graceful shutdown / draining, bulkheads / backpressure. Cross-ref `performance-backend.md` `BE-POOL-002` and `CFG-*`. Landing this resolves 034's forward-reference. Surfaced 2026-06-28 during the framework rule review.

- **Pin down degenerate `[rules] surfaces` configs on [033-rule-surface-setting](033-rule-surface-setting/spec.md)** (low-confidence findings parked in `specs/033-rule-surface-setting/review.md`). Two cases the implemented procedures leave unspecified: an explicitly **empty** list (`surfaces = []`) — "cross-only" vs. rejected-as-invalid, and distinct from unset = derive — and an **invalid member** (e.g. `"fullstack"`, a typo) — fail-fast vs. ignore-and-warn. A small `/gov:amend` on 033 (recommend: `[]` = cross-only documented explicitly; unknown member = fail-fast per `CFG-ENV-003`). Surfaced 2026-06-28 via `/gov:review` on 033.
