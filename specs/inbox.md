# Inbox

- review all specs.md and update the tags for each one
- slash commands don't show summary like built-in commands do. figure out if summary can be show in the autocomplete window
- consider adding optional frontmatter to tasks.md (and possibly plan.md) under the 013-text-first-artifacts schema. Open question whether a concrete consumer (validate rule, graph view, cross-cutting task query) needs it. Surfaced during 010 clarify; deferred until a consumer drives the requirement.
