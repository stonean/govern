---
description: Feature spec template for {project}.
---

# {project} feature template

Stub template shipped to the {project} adopter. Used by the
create-strategy entry in apply-manifest: only written when the
destination is absent so adopter edits survive a re-bootstrap.
