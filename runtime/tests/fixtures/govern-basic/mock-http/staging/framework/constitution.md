# Constitution

Framework-owned governance rules. Pinned in the apply-manifest entry
list so adopter edits to a previously-installed copy are never
overwritten. The fixture pre-seeds `project/framework/constitution.md`
before the run; apply-manifest records `skipped-pinned` for this
entry.
