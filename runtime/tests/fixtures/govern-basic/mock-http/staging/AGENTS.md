# AGENTS.md

Adopter-owned template for the {project} project. The
skip-if-conflict strategy seeds this file once and never edits it
afterward — so `{project}` here must stay literal. The substitutions
map IS passed to apply-manifest, but the skip-if-conflict strategy
suppresses substitution per primitive contract.
