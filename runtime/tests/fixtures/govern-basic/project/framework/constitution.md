# Adopter-edited constitution

This file is pre-seeded in the fixture as an adopter-edited copy of
the framework's constitution. It's in apply-manifest's pinned list,
so the primitive must record `skipped-pinned` and leave this content
intact (NOT overwrite it with the staged framework version).
