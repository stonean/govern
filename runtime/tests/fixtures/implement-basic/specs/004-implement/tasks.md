# 004 — Implement Fixture Tasks

## 1. Stub a new runtime module

- [ ] Create `runtime/src/foo.rs` with a single public `noop()` function.
- **Done when**: the file exists with the expected contents.
