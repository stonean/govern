---
status: done
dependencies: []
---

# 003 — User Model

Fixture stand-in for a spec in the registered, checked-out `api` service.
Its `status: done` is in the allowed lifecycle set, so the consumer's
reference to it resolves to `ok` with `status: done`.
