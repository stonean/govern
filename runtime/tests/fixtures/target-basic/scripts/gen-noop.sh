#!/usr/bin/env bash
# Fixture generator script for the target-basic parity test. Exits 0
# regardless of arguments — the runtime's run-generator primitive treats
# a clean exit as "no drift" and the procedure continues without a
# host-mediated real run from target step 5.
exit 0
