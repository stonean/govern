TODO: capture the LLM-driven `/gov:implement` output against
`runtime/tests/fixtures/implement-basic/` and replace this file's body
with the captured post-run state.

Capture recipe (maintainer-only, requires an LLM-driven host):

  1. cp -r runtime/tests/fixtures/implement-basic <a-clean-tempdir>
  2. cp framework/commands/implement.md <a-clean-tempdir>/framework/commands/
  3. cd <a-clean-tempdir> && git init && git add . && git commit -m chore
  4. Run `/gov:implement` in the LLM-driven host; confirm the
     planned → in-progress gate when prompted.
  5. After the run, record:
     a. The contents of <a-clean-tempdir>/specs/004-implement/tasks.md
        with the task-1 subtask checkbox flipped to checked.
     b. The set of paths the LLM modified outside specs/004-implement/.

Parity bound for `/gov:implement`:
  - strict-fields: `task-checkbox-state` — the tasks.md checkbox
    transitions must match the runtime-driven path exactly.
  - strict-files: `specs/{feature}/tasks.md` — the post-run tasks file
    must match byte-for-byte.
  - semantic-fields: `code-edits[].content` — the LLM's writeCode
    edit content is semantic; the runtime validates only the path
    against the write boundary.

Until this file is filled in, the parity test for `/gov:implement` is
recorded as SKIPPED and does not block CI.
