# Canonical names of runtime tools exposed by the optional `govern` runtime.
#
# Source of truth: framework/constitution.md §runtime-boundary
#
# Each non-blank, non-comment line is one tool name (matching MCP tool names
# or runtime binary names). The list is consumed by:
#
#   - .github/workflows/markdown-only-pipeline.yml (asserts no entry is on PATH)
#   - scripts/lint-tool-coverage.sh              (verifies references in
#                                                  framework/commands/*.md
#                                                  are wrapped in a graceful
#                                                  fallback)
#
# Tool names are case-sensitive exact-match strings. The `gov-rt:`
# namespace prefix is per spec 022's MCP naming-convention resolution —
# the colon disambiguates primitives from slash commands (`gov:<cmd>`)
# and guarantees no collision with PATH-visible binaries on stock
# runners.

gov-rt:read-spec
gov-rt:read-tasks
gov-rt:mark-task
gov-rt:mark-criterion
gov-rt:set-status
gov-rt:derive-boundary
gov-rt:check-stuck
gov-rt:validate-frontmatter
gov-rt:resolve-anchor
gov-rt:traverse-deps
gov-rt:check-rule-ids
gov-rt:run-generator
gov-rt:lint-markdown
gov-rt:gate-confirm
gov-rt:fetch-archive
gov-rt:extract-archive
gov-rt:substitute-templates
gov-rt:merge-claude-md
gov-rt:apply-manifest
gov-rt:enforce-manifest
gov-rt:merge-managed-block
gov-rt:create-scenario
gov-rt:append-task
